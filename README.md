Вариант №32 --- Интерпретатор конфигурационного языка → XML

------------------------------------------------------------------------
📌 Описание проекта

Этот проект --- интерпретатор конфигурационного языка.
Программа читает текстовый файл со специальным синтаксисом,
обрабатывает:

-   комментарии
-   объявления констант
-   вычисления констант
-   строки
-   числа (восьмеричные)
-   массивы
-   идентификаторы

и выводит результат в формате **красиво отформатированного XML**.

------------------------------------------------------------------------
📌 Запуск программы

    python main.py --input server_config.txt

Где `server_config.txt` --- входной файл с кодом варианта №32.

------------------------------------------------------------------------
📌 Особенности реализации

✔ Поддерживаемые комментарии

 > Однострочные:

    NB. текст комментария

> Многострочные:

    /+
       комментарий
    +/

Комментарии полностью удаляются на этапе лексинга.

------------------------------------------------------------------------
✔ Типы данных

1. Числа

Только восьмеричные:

    0o7
    0O12
    0o755

2. Строки

Строка начинается с `@"..."`:

    @"Hello"
    @"Строка с пробелами"

3. Массивы

Элементы разделяются `;`:

    [1; 2; @"hello"; 0o77]

4. Идентификаторы

    abc
    name123
    Value_X

------------------------------------------------------------------------
✔ Объявление констант

    (define name value)

Пример:

    (define a 0o77)
    (define greeting @"Hi")
    (define arr [1;2;3])

------------------------------------------------------------------------
✔ Использование констант

    .(name).

Пример:

    .(a).
    .(greeting).
    .(arr).

------------------------------------------------------------------------
📌 Вывод XML

Результат работы программы --- корректно отформатированный XML с
табуляцией:

<root>
        <item>
                <string>SERVER_NAME</string>
        </item><item>
                <string>web_app_01</string>
        </item><item>
                <string>LISTEN_PORT</string>
        </item><item>
                <number>520</number>
        </item><item>
                <number>520</number>
        </item><item>
                <string>TIMEOUT</string>
        </item><item>
                <number>511</number>
        </item><item>
                <string>ALLOWED_METHODS</string>
        </item><item>
                <array>
                        <string>GET</string>
                        <string>POST</string>
                        <string>HEAD</string>
                </array>
        </item><item>
                <string>ADMINS</string>
        </item><item>
                <array>
                        <string>john</string>
                        <string>admin, root</string>
                </array>
        </item>
</root>

------------------------------------------------------------------------
📌 Пример входного файла:

/+
Это конфигурация веб-сервера.
Используется многострочный комментарий.
+/

// Объявление констант на этапе трансляции
(define DEFAULT_PORT 0o1010)
(define ADMIN_LIST @"admin, root")

NB. Это однострочный комментарий

// Конфигурационные элементы
SERVER_NAME = @"web_app_01";
LISTEN_PORT = .(DEFAULT_PORT).; // Используем константу: 520 (0o1010 = 520)

TIMEOUT = 0o777; // Восьмеричное число: 511

ALLOWED_METHODS = [
    @"GET";
    @"POST";
    @"HEAD"
];

ADMINS = [
    @"john";
    .(ADMIN_LIST).
];

------------------------------------------------------------------------
📌 Особенности реализации и устойчивость

-   Программа **игнорирует мусорные символы**, включая кириллицу,
    PDF-артефакты и лишние символы `/`, `;`, `)` на верхнем уровне.
-   Лишние закрывающие скобки `)` **не приводят к ошибке**.
-   Лишние `;` также не ломают парсер.
-   Многострочные комментарии парсятся вручную (без регэкспов), что
    обеспечивает корректность.
-   Вывод полностью валиден для XML.

------------------------------------------------------------------------
📌 Автор

Цвигун К.С. ИКБО-67-24
